export default async function Loading(){
    return (
        <div>
            <h1 className="capitalize text-2xl font-bold text-blue-600 animate-pulse ">Loading... please wait</h1>
        </div>
    )}
